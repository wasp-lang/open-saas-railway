# Never copy host build artifacts into the image. The host node_modules holds
# platform-specific native bindings (e.g. rolldown) that break the linux build,
# and .wasp is regenerated by `wasp build` inside the container.
**/node_modules
app/.wasp
**/.env.server
**/.env.client
